import { Types } from './types/Types';
import {
  update,
  subscription,
  getStoreValue,
  addEventListener,
} from './golgua/Golgua';

const Golgua = {
  Types,
  update,
  subscription,
  getStoreValue,
  addEventListener,
};

export default Golgua;
