import { Types } from './types/Types';
import {
  subscription,
  update,
  updateWithTypes,
  setUpdateListener,
} from './golgua/Golgua';

const Golgua = {
  Types,
  subscription,
  update,
  updateWithTypes,
  setUpdateListener,
};

export default Golgua;
